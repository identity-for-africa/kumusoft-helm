apiVersion: v1
kind: Service
metadata:
  name: {{ include "payment-portal.fullname" . }}
  labels:
    {{- include "payment-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "payment-portal.selectorLabels" . | nindent 4 }}
