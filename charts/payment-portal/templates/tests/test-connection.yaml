apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "payment-portal.fullname" . }}-test-connection"
  labels:
    {{- include "payment-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "payment-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
